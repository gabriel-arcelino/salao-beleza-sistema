# Plano: Documentação Arquitetural v2.5

Objetivo: criar `plano-arquitetura-salao-beleza-v2_5.md` refletindo o estado real do projeto.

## Passos

1. Ler arquivos de referência (AGENTS.md, PROGRESS.md, v2_4.md, package.json, App.tsx, types.ts, pages/, lib/, migrations/, tests/, .spec/, onpspec.config.json, scripts/). [Feito na exploração]
2. Validar nomes de RPCs (fn_fechar_comanda, fn_fechar_competencia_comissao, fn_cancelar_comanda, fn_estornar_pagamento, fn_calcular_cmv, fn_relatorio_caixa, fn_relatorio_comissao) e migrações até 0013.
3. Confirmar RLS: `auth_helpers.current_salon_id()`.
4. Confirmar stack: React 18, Vite, TypeScript, Supabase, PostgreSQL, Vitest, Testing Library, jsdom. Tailwind/Recharts/Lucide não estão em package.json — marcar como não adotados.
5. Confirmar estrutura: `.spec/`, `.claude/`, `.kilo/`, `scripts/`, `tests/`, `supabase/`, `AGENTS.md`, `PROGRESS.md`, `onpspec.config.json`, `vitest.config.ts`.
6. Confirmar testes: pgTAP em `supabase/tests/`; Vitest + Testing Library (`tests/`); adapter `scripts/onp-combined-verify.cjs`; evidência Fase 4 (66 pgTAP + 8 Vitest = 74, AC-001..011, exit code 0).
7. Confirmar status: Fase 1-4 concluídas; próxima Fase 5 (UI/UX e consolidação); Fase 6 (Produção e governança) depois.
8. Confirmar alertas de estoque: estoque negativo não bloqueia venda; alerta visual in-app (Dashboard, RelatorioEstoquePage); e-mail Resend planejado; WhatsApp automático fora do escopo.
9. Escrever `plano-arquitetura-salao-beleza-v2_5.md` com cabeçalho corrigido, arquitetura baseada em App.tsx/pages/lib/supabaseClient/RLS/RPC, stack sincronizada, RLS com nome real, frontend funcional/prototípico com evolução planejada, relatórios implementados com contratos reais, migrations atualizadas até 0013, estrutura factual, testes documentados com evidência Fase 4, roadmap substituído, alertas de estoque diferenciados, RPCs documentadas como implementadas, seção de estado de implementação e fonte da verdade, regras de manutenção.
10. Revisar divergências (referências a arquitetura antiga como camadas físicas não existentes, funcionalidades futuras tratadas como implementadas, nomes antigos de funções).
11. Executar validação se aplicável (não alterar código; `onp-spec audit --ci` apenas se o escopo incluir e for seguro).
12. Registrar resultado final: arquivo criado, ajustes principais, divergências corrigidas, validações, pontos pendentes.

Restrições: não alterar código; não apagar v2_4.md; não instalar dependências; não alterar migrations/testes.
